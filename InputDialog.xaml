<Window x:Class="Индивидуальное_задание.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Диалоговое окно" Height="205" Width="300">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Margin="10">
                <TextBlock FontWeight="Bold" x:Name="QuestionTextBlock" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                <TextBox x:Name="ResponseTextBox" Height="86" />
            <Button x:Name="okButton" Click="okButton_Click" IsDefault="True" Margin="88,0" Content="ОК"/>
            <Button x:Name="cancelButton" IsCancel="True" Margin="88,0" Content="Отмена" RenderTransformOrigin="0.513,1.357"/>
        </StackPanel>
        </Grid>
</Window>
